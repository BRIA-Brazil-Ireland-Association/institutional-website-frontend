import { SectionReveal } from "../../ui/section-reveal";

export function HeroBanner() {
  return (
    <SectionReveal className="flex min-h-96 flex-col items-center justify-center bg-zinc-100 text-center text-black">
      Hero
    </SectionReveal>
  );
}
